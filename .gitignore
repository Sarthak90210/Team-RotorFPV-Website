# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.env
.env.*
# Keep the example templates tracked (they contain no real secrets)
!.env.example
!server/.env.example

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Backend Secrets
serviceAccountKey.json
server/serviceAccountKey.json
server/.env
server/node_modules
.firebase/

.claude

# Raw 3D model source files (~83 MB) — kept locally, not tracked in git.
# Optimized models are served from public/ and Supabase Storage instead.
model-source/
